# Point this at the real backend once it's running.
VITE_API_BASE_URL=http://localhost:5000/api

# Set to "false" the moment a real endpoint is ready — every service file
# in src/services/api already branches on this, so no component changes needed.
VITE_USE_MOCK=true
