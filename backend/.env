PORT=5000

# PostgreSQL connection (matches a local pgAdmin/Postgres server)
PGHOST=localhost
PGPORT=5432
PGDATABASE=transitops
PGUSER=postgres
PGPASSWORD=Madhavpostgre

JWT_SECRET=change_this_to_a_long_random_secret
JWT_EXPIRES_IN=8h

CORS_ORIGIN=http://localhost:5173
